package main

import "fmt"

// open a connection (create the server)
// listen to incoming POST requests on a given route
// create a temp file
// save data (bytes) into the file

func main() {

	fmt.Printf("image uploader")
}
